# Northstar Banking UI

Angular frontend for the Northstar Banking dashboard.

## Run

```bash
npm install
ng serve --proxy-config proxy.conf.json
```

## Build

```bash
npm run build
```

## Backend API

This UI expects the backend API to run at `http://localhost:5200`.
